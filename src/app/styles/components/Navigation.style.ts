import styled from '@emotion/styled';
import theme from '../theme';

export const Wrapper = styled.div({
  display: 'flex',
  justifyContent: 'space-between',
});

export const Link = styled.div({ padding: theme.spacing[2] });
